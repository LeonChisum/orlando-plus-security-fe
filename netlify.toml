[context.production]
  environment = { REACT_APP_NETLIFY_CONTEXT = "production" }

[context.deploy-preview]
  environment = { REACT_APP_NETLIFY_CONTEXT = "deploy-preview" }

[context.branch-deploy]
  environment = { REACT_APP_NETLIFY_CONTEXT = "branch-deploy" }
